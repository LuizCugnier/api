version: '3.8'

services:
  db:
    image: postgres
    container_name: postgres-rinha
    ports:
      - "5454:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rinha-db
